import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        height: "60px",
        width: "100%",
        display: "flex",
        alignItems: "center",
        background: "rgba(254, 158, 0, 0.55)",
      }}
    >
      Header
    </Box>
  );
};

export default Header;
